colorama==0.4.6
crcmod==1.7
et_xmlfile==2.0.0
func_timeout==4.3.5
iniconfig==2.0.0
Jinja2==3.1.5
MarkupSafe==3.0.2
numpy==2.2.0
openpyxl==3.1.5
packaging==24.1
pluggy==1.5.0
pymodbus==3.7.4
pyserial==3.5
pytest==8.3.3
pytest-html==4.1.1
pytest-metadata==3.1.1
xlwt==1.3.0
selenium~=4.38.0
PyYAML~=6.0.2
pandas~=2.3.1
# 以下原为浮动下限（>=），同一份代码在不同机器会解析出跨大版本的不同实现，曾导致
# pytest-playwright 由旧名 playwright_instance 改名为 playwright，使 conftest 覆盖失效、
# 双 sync_playwright 抢占事件循环报错。统一收紧为兼容发布区间（~=：锁定 minor，仅放行
# patch），保证 team 内可复现。升级前先在 conftest 验证 pytest-playwright 的 fixture 命名。
BAC0~=2025.9.15
pycomm3~=1.2.16
pytest-playwright~=0.7.2
playwright~=1.58.0
pytest-xdist>=3.3.0
python-dotenv~=1.2.1
allure-pytest~=2.16.0    # conftest.py 直接 import allure，属硬依赖，原文件漏列
amqtt==0.11.3
pyftpdlib==26.1.2
# acuview_auto_testcase_generate skill (ctl_acuview 引擎) 新增依赖
pywinauto>=0.6.8
uiautomation>=2.0.18
comtypes>=1.2
pyautogui>=0.9.54
opencv-python>=4.8
pillow>=10.0
pytesseract>=0.3.13     # 仅 read_compare 读 GUI 显示值比对需要(另需装 Tesseract-OCR 引擎)
