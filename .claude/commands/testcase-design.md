---
name: testcase-design
description: |
  将测试策略脑图或测试点列表转化为规范的 Excel 测试用例文件（.xlsx）。
  适用于任意项目的测试用例生成任务。

  当用户提到以下任意情况时，必须调用本 skill，不得跳过：
  - 要求生成测试用例、写用例、整理用例
  - 要求将测试策略/脑图/测试点转化为 Excel
  - 要求导出 .xlsx 测试用例文件
  - 任何"帮我写用例""生成用例表""输出测试用例"类请求

  本 skill 包含完整的：
  1. 测试用例内容规范（列定义、编号规则、优先级、拆分原则）
  2. Excel 格式规范（字体、颜色、边框、换行、列顺序）
  3. Excel 文件生成代码模板

  不得跳过本 skill 直接输出用例内容或生成文件。
---

# 测试用例 Excel Skill

## 概述

本 skill 将测试点/脑图转化为可直接执行的 Excel 测试用例，格式严格对齐 AC-HMI 测试用例规范 v2.4。

**适用场景：** 任意项目，从脑图测试点、需求文档、测试策略生成标准 Excel 用例表。

**执行顺序（每次必须按序执行）：**
1. 阅读本文档第一步至第六步的全部规范
2. 阅读下方「用例内容规范与拆分示例」章节
3. 起草用例内容，逐条核对检查清单
4. 阅读下方「Excel 文件生成规范」章节，生成 .xlsx 文件并提供下载

---

## 分析隔离原则（每次必须遵守）

**每次处理新模块时，必须重新读取当前文档原始内容，严禁携带历史分析信息。**

- 每次为特定模块生成用例前，**必须从当前测试策略/需求文档重新读取该模块的原始内容**，不得依赖对话记忆中的旧版内容
- **严禁**将本次对话中对其他模块的分析结论带入当前任务
- **严禁**引用历史对话中对同一功能的旧版分析（需求或测试点可能已更新）
- 若当前读取内容与历史记忆存在任何不一致，**以当前读取内容为唯一依据**，不做融合

---

## 第一步：列定义与顺序

Excel 列按以下顺序输出，**不得调整顺序、不得增减列**：

| 列名 | 说明 |
|------|------|
| 模块 | 一级功能模块名称，**加粗** |
| 子模块 | 二级功能模块名称，**加粗** |
| FTS编号 | 格式见编号规则 |
| FTS名称(用例编号) | 简短描述，与用例标题保持一致 |
| 用例编号 | 格式见编号规则 |
| 用例标题 | 简短中文标题，与 FTS名称 保持一致 |
| 预置条件 | 固定文案（见下） |
| 测试步骤 | 多行编号，步骤数量不限 |
| 预期结果 | 多行编号，与步骤对应 |
| 用例级别 | LV0~LV4 |
| 测试结果 | 默认留空 |
| 测试负责人 | 默认留空 |
| 备注 | 默认留空 |
| 完成自动化 | 默认留空 |
| 问题单来源 | 默认留空 |

---

## 第二步：编号规则

**项目缩写生成规则：**
- 从项目名称中提取英文和数字核心标识
- 去掉空格、连字符、下划线等分隔符后转大写
- 示例：`AcuRev-4100-WEB2` → `ACUREV4100WEB2`，`AC-HMI` → `ACHMI`
- **不得固定写死为 ACHMI 或任何历史项目代号**

**编号格式：**
```
用例编号：TestCase_<项目缩写>_<一级模块3位>_<二级模块3位>_<用例3位>
FTS编号：FTS_<项目缩写>_<一级模块3位>_<二级模块3位>_<用例3位>
```
FTS编号 = 用例编号将前缀 `TestCase_` 替换为 `FTS_`，其余完全一致。

---

## 第三步：固定规则

- **预置条件**：统一固定写为 `设备已正常上电，相关服务正常启动`，不展开
- **测试步骤**：按 `1. 2. 3.` 多行编写，步骤数量不限
- **预期结果**：尽量与步骤逐条对应，按 `1. 2. 3.` 多行编写；可跳号但不得与步骤错位
- **FTS名称 / 用例标题**：保持简短，详细列表写入步骤和预期结果，不堆在标题里

---

## 第四步：用例级别（LV0~LV4）

| 级别 | 定义 |
|------|------|
| LV0 | 功能级冒烟。每个功能模块核心正向主流程的最小闭环，验证功能基本可用，可直接给开发自测。每个功能模块 1~2 条 |
| LV1 | 完整正向验证。合法参数配置（含有效边界值）、完整正向路径、关键配置生效与结果验证 |
| LV2 | 异常与非法输入。非法值、超出范围值、空值、错误提示、失败处理、关键异常恢复场景 |
| LV3 | 补充验证。兼容性、模式切换、次高频流程、联动规则、多功能共存、非主路径交互 |
| LV4 | 低优先级补充。信息展示完整性、低频细项、UI 细节、长稳或非阻塞问题 |

**分配原则：**
- LV0 只覆盖"能进入页面 + 完成一次核心操作 + 结果正确"的最小路径，每个功能模块严格控制在 1~2 条
- LV1 覆盖合法边界值（最小值、最大值）和完整正向路径，有效值配置成功属于 LV1
- LV2 覆盖非法值和异常场景，无效边界值（超出范围、空值）归 LV2，不归 LV1
- LV3、LV4 覆盖兼容性、低频路径、展示类细节，不影响核心功能可用性
- 只读信息展示、低频路径、长稳测试归 LV4，不标 LV0

---

## 第五步：用例拆分原则

- **脑图一个最小分支 ≠ 一个用例**，应按校验维度、边界值、正/异常场景拆分
- 若脑图分支末尾标有「X个用例」，**必须严格按该数量生成**，不得增减
- 参数范围类测试点，自动按等价类/边界值生成至少 2 组：1组有效值 + 1组无效值
- 涉及 IP / Subnet Mask / Gateway / DNS1 / DNS2 等常见参数，补充可执行的具体测试值

---

## 第五步补充：「待确认」主题跳过规则

遍历 XMind 所有主题时，若某节点标题包含**「待确认」**（任意位置、任意大小写），处理规则如下：

- **该节点及其所有子节点**全部跳过，不生成任何测试用例
- 跳过范围：分支主题、细分主题、叶子节点，只要标题含「待确认」即跳过整棵子树
- 不在 Excel 中留占位行，不计入用例总数
- 若整个二级分组标题含「待确认」，该分组下所有用例均跳过

> **示例：** XMind 中存在节点 `Meter Point Configuration 待确认`，则该节点及其下全部子节点均不输出用例。

---

## 第六步：生成 Excel 文件

参考下方「Excel 文件生成规范」章节。

**格式规范摘要：**
- 字体：微软雅黑，10pt，全表统一
- 表头行填充色：绿色 个性色6（RGB `#70AD47`）
- 模块、子模块列内容：**加粗**
- 所有单元格：自动换行 + 垂直居中 + 全边框
- 工作表命名：当前测试主题名称，如 `01控制与导航`
- 输出路径：`/mnt/user-data/outputs/`
- 生成后调用 `present_files` 提供下载

---

## 生成检查清单

输出前逐项确认：
- [ ] 列顺序与 columns_in_order 完全一致，共15列
- [ ] 项目缩写根据当前项目名动态生成，未写死
- [ ] FTS编号与用例编号仅前缀不同，其余一致
- [ ] 预置条件统一为固定文案
- [ ] 测试步骤按 1. 2. 3. 编写，步骤数量不限
- [ ] 步骤与预期结果编号对应，无错位
- [ ] 标注「X个用例」的分支严格按数量生成
- [ ] 标题含「待确认」的主题及其全部子节点已跳过，未生成任何用例
- [ ] 参数范围类已拆出有效值+无效值两组
- [ ] 脑图列出的字段/参数/菜单列表完整写入步骤，未用"等"代替
- [ ] FTS名称/用例标题保持简短
- [ ] 用例级别已按 LV0~LV4 标注
- [ ] 测试结果/负责人/备注/自动化/问题单来源 均留空
- [ ] Excel 字体微软雅黑10pt、表头绿色、模块列加粗、全边框、自动换行

---

# 用例内容规范与拆分示例

## 核心内容原则

1. **完整可执行**：执行人员只看用例即可完成测试，不得再依赖脑图或需求文档
2. **列表完整写出**：脑图已列出的菜单、页面项、参数、字段、状态列表，用例中必须完整体现，不得用"等""相关项目""完整显示"代替
3. **标题简短，细节入步骤**：FTS名称/用例标题只写简要描述，详细列表写进测试步骤和预期结果
4. **步骤简洁**：单条步骤一句话，不写大段；单条用例不超过4步，超过则拆分

---

## 拆分规则详解

### 规则1：按校验维度拆分
一个脑图分支同时包含多个独立校验维度时，拆成多个用例。

**示例：**
- 脑图：`查看4100参数：单位、精度、取值范围与上位机一致`
- 拆分为：
  - 用例1：对比参数**单位**与上位机一致
  - 用例2：对比参数**精度**与上位机一致
  - 用例3：对比参数**取值范围**与上位机一致

---

### 规则2：按边界值拆分（等价类+边界值分析）
测试点包含明确参数范围时，自动拆出至少2组：

| 组别 | 取值策略 |
|------|----------|
| 有效值组 | 边界内合法值，优先取边界值（最小值、最大值）和典型中间值 |
| 无效值组 | 边界外非法值，优先取刚好超出边界的值 |

**示例1：**
- 脑图：`配置 Nominal Voltage: 1~690`
- 拆分为：
  - 用例1（有效值）：配置 Nominal Voltage = 1、500、690，保存成功
  - 用例2（无效值）：配置 Nominal Voltage = 0、691，阻止保存并提示

**示例2：**
- 脑图：`Advertised APDU Timeout 默认3秒，范围3~60`
- 拆分为：
  - 用例1（有效值）：配置 Advertised APDU Timeout = 3s、60s，保存成功
  - 用例2（无效值）：配置 Advertised APDU Timeout = 2s、61s，阻止保存并提示

---

### 规则3：参数遍历拆分
涉及多个同类参数遍历时，按参数逐条拆分，并补充具体可执行测试值。

**示例：**
- 脑图：`遍历配置Ethernet1：IP、Subnet Mask、Gateway、DNS1、DNS2，配置保存后可以重连成功`
- 拆分为：
  - 用例1：配置 Ethernet1 IP = `192.168.10.50`，保存后重连成功
  - 用例2：配置 Ethernet1 Subnet Mask = `255.255.255.0`，保存后重连成功
  - 用例3：配置 Ethernet1 Gateway = `192.168.10.1`，保存后重连成功
  - 用例4：配置 Ethernet1 DNS1 = `8.8.8.8`，保存后重连成功
  - 用例5：配置 Ethernet1 DNS2 = `1.1.1.1`，保存后重连成功

> 常用测试值参考：IP `192.168.10.50`，Subnet Mask `255.255.255.0`，Gateway `192.168.10.1`，DNS1 `8.8.8.8`，DNS2 `1.1.1.1`

---

### 规则4：正常/异常场景拆分
同一功能的正常场景和异常场景分为独立用例。

**示例：**
- 脑图：`用户名长度限制 1~20 字符`
- 拆分为：
  - 用例1（LV1）：输入合法用户名（1字符、20字符），保存成功
  - 用例2（LV2）：输入0字符（空）或21字符，阻止保存并提示错误

---

### 规则5：强制用例数
脑图分支末尾标有「X个用例」时，必须严格生成 X 条用例，不得增减。

**示例：**
- 脑图：`查看SN、MAC1、MAC2、WiFi MAC是否正确，并保证都是只读状态。2个用例`
- 严格生成2条：
  - 用例1：查看 SN、MAC1、MAC2、WiFi MAC 显示值正确
  - 用例2：验证 SN、MAC1、MAC2、WiFi MAC 均为只读，不可编辑

---

## 步骤与预期结果写法规范

### 格式
```
测试步骤：
1. <动作描述>
2. <动作描述>
3. <动作描述>

预期结果：
1. <对应步骤1的预期>
2. <对应步骤2的预期>
3. <对应步骤3的预期>
```

### 编号对应规则
- 预期结果编号必须与步骤编号对应
- 预期结果可跳号（如只有步骤2、3有预期，写 `2. xxx  3. xxx`）
- **不得出现编号错位**（如步骤有3步，预期结果编号却从1跳到4）

### 步骤写法要求
- 每步一句话，动词开头，如：`进入`、`输入`、`点击`、`查看`、`配置`
- 不写大段描述，不在步骤里嵌套子列表
- 超过4步时优先拆分为多个更聚焦的用例

---

## 完整用例示例

```
模块：        通信
子模块：      BACnet/IP 参数校验
FTS编号：     FTS_ACHMI_COM_001_001_001
FTS名称：     BACnet Port 合法值配置
用例编号：    TestCase_ACHMI_COM_001_001_001
用例标题：    BACnet Port 合法值配置
预置条件：    设备已正常上电，相关服务正常启动
测试步骤：    1. 进入设置 -> 通信 -> BACnet/IP 配置页面
              2. 配置 BACnet Port = 47808（最小值）、49000（最大值），分别保存
              3. 使用 BACnet 客户端连接设备
预期结果：    1. 成功进入 BACnet/IP 配置页面
              2. 两次配置均保存成功，页面展示值与输入值一致
              3. BACnet 客户端可以正常连接并通信
用例级别：    LV1
```

```
模块：        通信
子模块：      BACnet/IP 参数校验
FTS编号：     FTS_ACHMI_COM_001_001_002
FTS名称：     BACnet Port 非法值配置
用例编号：    TestCase_ACHMI_COM_001_001_002
用例标题：    BACnet Port 非法值配置
预置条件：    设备已正常上电，相关服务正常启动
测试步骤：    1. 进入设置 -> 通信 -> BACnet/IP 配置页面
              2. 配置 BACnet Port = 47807（低于最小值），点击保存
              3. 配置 BACnet Port = 49001（超出最大值），点击保存
预期结果：    2. 阻止保存，页面提示端口号超出范围
              3. 阻止保存，页面提示端口号超出范围
用例级别：    LV2
```

---

# Excel 文件生成规范

## 依赖库

```bash
pip install openpyxl --break-system-packages
```

---

## 完整生成模板

```python
import openpyxl
from openpyxl.styles import (
    Font, PatternFill, Alignment, Border, Side
)

# ============================================================
# 配置区：每次生成前修改这里
# ============================================================
OUTPUT_PATH = '/mnt/user-data/outputs/测试用例_模块名.xlsx'
SHEET_NAME  = '01测试模块名'   # 工作表命名：测试主题名称

# 列顺序（不得调整）
COLUMNS = [
    '模块', '子模块', 'FTS编号', 'FTS名称(用例编号)',
    '用例编号', '用例标题', '预置条件',
    '测试步骤', '预期结果', '用例级别',
    '测试结果', '测试负责人', '备注', '完成自动化', '问题单来源'
]

# 测试用例数据（按 COLUMNS 顺序填写）
# 测试步骤和预期结果用 \n 换行
CASES = [
    {
        '模块':           '通信',
        '子模块':         'BACnet/IP 参数校验',
        'FTS编号':        'FTS_ACHMI_COM_001_001_001',
        'FTS名称(用例编号)': 'BACnet Port 合法值配置',
        '用例编号':       'TestCase_ACHMI_COM_001_001_001',
        '用例标题':       'BACnet Port 合法值配置',
        '预置条件':       '设备已正常上电，相关服务正常启动',
        '测试步骤':       '1. 进入设置 -> 通信 -> BACnet/IP 配置页面\n2. 配置 BACnet Port = 47808，点击保存\n3. 配置 BACnet Port = 49000，点击保存',
        '预期结果':       '1. 成功进入配置页面\n2. 保存成功，展示值与输入值一致\n3. 保存成功，展示值与输入值一致',
        '用例级别':       'LV1',
        '测试结果': '', '测试负责人': '', '备注': '', '完成自动化': '', '问题单来源': '',
    },
    # ... 继续添加用例
]

# ============================================================
# 样式定义
# ============================================================
HEADER_FILL  = PatternFill('solid', fgColor='70AD47')   # 绿色 个性色6
NORMAL_FONT  = Font(name='微软雅黑', size=10)
BOLD_FONT    = Font(name='微软雅黑', size=10, bold=True)
HEADER_FONT  = Font(name='微软雅黑', size=10, bold=True)

ALIGN_WRAP   = Alignment(wrap_text=True, vertical='center', horizontal='left')
ALIGN_CENTER = Alignment(wrap_text=True, vertical='center', horizontal='center')

THIN = Side(style='thin')
ALL_BORDERS = Border(left=THIN, right=THIN, top=THIN, bottom=THIN)

BOLD_COLS = {'模块', '子模块'}   # 这两列内容加粗

# ============================================================
# 列宽参考（可按需调整）
# ============================================================
COL_WIDTHS = {
    '模块': 16, '子模块': 18, 'FTS编号': 30,
    'FTS名称(用例编号)': 28, '用例编号': 30, '用例标题': 28,
    '预置条件': 36, '测试步骤': 50, '预期结果': 50,
    '用例级别': 10, '测试结果': 12, '测试负责人': 14,
    '备注': 14, '完成自动化': 12, '问题单来源': 14,
}

# ============================================================
# 生成函数
# ============================================================
def build_excel(cases, output_path, sheet_name):
    wb = openpyxl.Workbook()
    ws = wb.active
    ws.title = sheet_name

    for col_idx, col_name in enumerate(COLUMNS, start=1):
        cell = ws.cell(row=1, column=col_idx, value=col_name)
        cell.font        = HEADER_FONT
        cell.fill        = HEADER_FILL
        cell.alignment   = ALIGN_CENTER
        cell.border      = ALL_BORDERS
        ws.column_dimensions[
            openpyxl.utils.get_column_letter(col_idx)
        ].width = COL_WIDTHS.get(col_name, 16)

    for row_idx, case in enumerate(cases, start=2):
        for col_idx, col_name in enumerate(COLUMNS, start=1):
            value = case.get(col_name, '')
            cell  = ws.cell(row=row_idx, column=col_idx, value=value)
            cell.font      = BOLD_FONT if col_name in BOLD_COLS else NORMAL_FONT
            cell.alignment = ALIGN_WRAP
            cell.border    = ALL_BORDERS

        max_lines = max(
            str(case.get(c, '')).count('\n') + 1
            for c in ['测试步骤', '预期结果', '预置条件']
        )
        ws.row_dimensions[row_idx].height = max(18, max_lines * 18)

    ws.row_dimensions[1].height = 22
    ws.freeze_panes = 'A2'

    wb.save(output_path)
    print(f'已生成：{output_path}')

build_excel(CASES, OUTPUT_PATH, SHEET_NAME)
```

---

## 关键注意事项

| 事项 | 说明 |
|------|------|
| 换行符 | 测试步骤/预期结果用 `\n` 分隔，openpyxl 会自动展示为多行（需开启 wrap_text） |
| 表头颜色 | `PatternFill('solid', fgColor='70AD47')` 对应"绿色 个性色6" |
| 加粗列 | 模块、子模块列使用 `BOLD_FONT`，其余列使用 `NORMAL_FONT` |
| 字体 | 全表统一 `微软雅黑 10pt` |
| 边框 | 所有单元格（含表头）使用 `ALL_BORDERS` |
| 输出路径 | 固定写入 `/mnt/user-data/outputs/`，生成后调用 `present_files` |
| 工作表命名 | 使用当前测试主题，如 `01控制与导航`、`02通信协议` |
| 空列 | 测试结果/负责人/备注/自动化/问题单来源 写入空字符串 `''` |

---

## 编号生成辅助函数

```python
def make_ids(project_name, module_3, submodule_3, seq_3):
    import re
    abbr = re.sub(r'[^A-Za-z0-9]', '', project_name).upper()
    suffix = f'{module_3}_{submodule_3}_{seq_3}'
    return (
        f'FTS_{abbr}_{suffix}',
        f'TestCase_{abbr}_{suffix}',
    )
```
